{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch BerlinClock tests",
      "type": "go",
      "request": "launch",
      "mode": "test",
      "env": {
        "GOPATH": "${workspaceRoot}"
      },
      "program": "${workspaceRoot}/test"
    }
  ]
}